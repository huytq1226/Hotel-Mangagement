from django.apps import AppConfig

# BookingConfig class định nghĩa cấu hình cho ứng dụng 'booking'
# Đây là file cấu hình Django app, được sử dụng để đăng ký app với project
class BookingConfig(AppConfig):
    name = 'booking'  # Tên của ứng dụng
